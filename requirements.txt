arxiv==2.1.3
pymupdf==1.28.2
sentence-transformers>=3.0.1
faiss-cpu==1.15.0
fastapi>=0.114.0
uvicorn[standard]>=0.30.6
numpy>=1.26.4
tqdm>=4.66.5
python-dotenv>=1.0.1
pytest>=8.3.2

# Hybrid retrieval (week 5). SQLite FTS5 needs no package -- it ships with
# Python's sqlite3 -- so rank_bm25 is here only as the second BM25 backend the
# evaluation compares FTS5 against.
rank_bm25>=0.2.2

# For running/regenerating notebooks/retrieval_demo.ipynb
nbformat>=5.10.4
nbclient>=0.10.0
ipykernel>=6.29.5
pandas>=2.2
matplotlib>=3.9

# Week 7 synthetic Q&A generation (scripts/generate_synthetic_qa.py). Only the client
# for the provider you set via LLM_PROVIDER is required at runtime; both are listed so
# either path installs cleanly. scripts/build_sft_dataset.py needs neither.
openai>=1.40
anthropic>=0.40
